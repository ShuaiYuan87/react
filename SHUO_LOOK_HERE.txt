The code is in FlyOtter
run ./node_modules/.bin/browserify --debug --transform reactify ./index.js > ./bundle.js first when you change something
run python -m SimpleHTTPServer in the root folder to start the server and hit localhost:8000 in the browser
